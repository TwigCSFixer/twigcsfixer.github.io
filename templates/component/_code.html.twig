<div class="code-window">
    <div class="code-window__header">
        <span class="code-window__dot"></span>
        <span class="code-window__dot"></span>
        <span class="code-window__dot"></span>
    </div>
    <div class="code-window__body">
        {% if block('code') is defined %}
        <pre class="code-window__code"><code>{{- block('code')|escape -}}</code></pre>
        {% endif %}
    </div>
</div>
